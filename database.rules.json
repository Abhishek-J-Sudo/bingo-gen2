{
  "rules": {
    "bingo-game": {
      ".read": true,
      ".write": false,
      "boards": {
        "$boardId": {
          ".write": "!data.exists() || data.child('sessionId').val() === newData.child('sessionId').val()"
        }
      },
      "assignedNames": {
        "$sessionId": {
          ".write": true
        }
      }
    }
  }
}
